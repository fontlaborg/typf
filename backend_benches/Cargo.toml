[package]
name = "backend-benches"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
criterion = "0.4"
typf-api = { path = "../crates/typf-api" }
typf-core = { path = "../backends/typf-core" }

[[bench]]
name = "speed"
harness = false
