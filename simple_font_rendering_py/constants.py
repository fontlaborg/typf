# this_file: reference/renderers/constants.py
"""
Rendering constants shared across renderer backends.
"""

# Default canvas dimensions (pixels)
RENDER_WIDTH = 3000
RENDER_HEIGHT = 1200

# Default font size (points)
DEFAULT_FONT_SIZE = 100

# Baseline positioning ratio (relative to canvas height)
# Baseline Y = height * RENDER_BASELINE_RATIO
RENDER_BASELINE_RATIO = 0.75
